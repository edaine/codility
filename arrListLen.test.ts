import { expect, test } from "@jest/globals";
import { solution } from "./arrListLen";

describe("solution template", () => {
  // --- Examples from the problem description ---
  test("should return ...", () => {
    expect(solution([1, 4, -1, 3, 2])).toBe(4);
  });

  test("should return ...", () => {
    expect(solution([1, 4, -1, 3, 2, 5])).toBe(4);
  });
});
